<?xml version="1.0" encoding="UTF-8"?>
<ContentView xmlns="http://xamarin.com/schemas/2014/forms" 
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             xmlns:utils="clr-namespace:Tp8.Utils"
             x:Class="Tp8.Views.HeaderView"
             BindingContext="{Binding HeaderViewModel, Source={StaticResource Locator}}">
    <ContentView.Content>
        <Grid Margin="10, 25">
            <Grid.ColumnDefinitions>
                <ColumnDefinition/>
                <ColumnDefinition/>
                <ColumnDefinition/>
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition/>
            </Grid.RowDefinitions>
            <ImageButton Grid.Row="0" Grid.Column="0" Source="{utils:ImageFromResources Tp8.Images.pencil.png}" VerticalOptions="Fill" HorizontalOptions="Fill"></ImageButton>
            <Label Grid.Row="0" Grid.Column="1" TextColor="White" Text="Accueil" FontSize="20" FontAttributes="Bold" VerticalTextAlignment="Center" HorizontalTextAlignment="Center"></Label>
            <ImageButton Grid.Row="0" Grid.Column="2" Command="{Binding SearchClick}" Source="{utils:ImageFromResources Tp8.Images.search.png}"></ImageButton>
        </Grid>
    </ContentView.Content>
</ContentView>