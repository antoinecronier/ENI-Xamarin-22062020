<?xml version="1.0" encoding="UTF-8"?>
<ContentView xmlns="http://xamarin.com/schemas/2014/forms" 
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             x:Class="Tp7Correction.Views.SearchView"
             BindingContext="{Binding SearchViewModel, Source={StaticResource Locator}}">
  <ContentView.Content>
        <StackLayout>
            <StackLayout Orientation="Horizontal" HorizontalOptions="StartAndExpand">
                <Label Text="Date" HorizontalOptions="CenterAndExpand"/>
                <DatePicker Date="{Binding Search.SearchDate}" HorizontalOptions="CenterAndExpand"/>
            </StackLayout>
            <StackLayout Orientation="Horizontal" HorizontalOptions="StartAndExpand">
                <Label Text="Utilisateur" HorizontalOptions="CenterAndExpand"/>
                <Entry Text="{Binding Search.Username}" HorizontalOptions="CenterAndExpand"/>
            </StackLayout>
            <Button Text="Rechercher" Command="{Binding SearchClick}"/>
        </StackLayout>
  </ContentView.Content>
</ContentView>